export const TRANSMISSION_TYPE = Object.freeze({
  AUTOMATIC: "Automatic",
  MANUAL: "Manual",
});

export const IMAGE_ALTS = Object.freeze({
  WHEEL: "wheel",
  TIRE: "tire",
  GAS: "gas",
});

export const IMAGE_DIMENSIONS = Object.freeze({
  WIDTH: 20,
  HEIGHT: 20,
});
